"use client";

import { BoxContent } from "@/src/shared/components";
import styled from "@emotion/styled";

export const SelfPictureContainer = styled(BoxContent)`
  gap: 8px;
`;

export const InputPicture = styled.input`
  display: none;
`;
